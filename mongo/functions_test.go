package mongo

import "testing"

func TestMongoFind(t *testing.T) {
	tests := []struct {
		name string
	}{
		// TODO: Add test cases.
	}
	for range tests {
		t.Run(tt.name, func(t *testing.T) {
			MongoFind()
		})
	}
}
